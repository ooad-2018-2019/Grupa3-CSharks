@model IEnumerable<SeekAndFind.Models.FakultetiAplikanta>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fakultet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fakultet.FakultetId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FakultetiAplikantaId">Edit</a> |
            </td>
        </tr>
}
    </tbody>
</table>
